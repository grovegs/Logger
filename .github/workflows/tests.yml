name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  tests:
    uses: grovegs/workflows/.github/workflows/godot-package-tests.yml@v1.0.25
    with:
      core-test-project: tests/GroveGames.Logger.Tests
      godot-test-project: tests/GroveGames.Logger.Godot.Tests
      global-json-file: global.json
